# Classified findings for Blur my Shell
# Source: docs/internal/field-test-blur-my-shell.md
findings:
  # FAILs — Fixed False Positives
  - id: "init/shell-modification::GObject.registerClass at module scope"
    classification: fp
    notes: "13 FPs — GObject.registerClass returns a class constructor, not an instance. Fixed: exemption in check-init.py."
  - id: "file-structure/extension.js::src/ layout"
    classification: fp
    notes: "Extension uses src/ subdirectory layout. Fixed: src/ fallback in ego-lint.sh."
  # FAILs — True Positives
  - id: "R-DEPR-06::Tweener usage"
    classification: tp
    notes: "appfolders.js uses imports.tweener.tweener — would crash on GNOME 46+. 4 line hits."
  - id: "R-VER47-01::Clutter.Color"
    classification: borderline
    notes: "appfolders.js has ternary runtime guard (Clutter.Color ? ... : Cogl.Color), but file crashes before reaching this code due to Tweener import"
  # WARNs — False Positives
  - id: "resource-tracking/destroy-not-called::disable() not recognized"
    classification: fp
    notes: "63 hits — components use .disable() not .destroy() as cleanup. Resource graph only recognizes destroy()."
  - id: "quality/constructor-resources::pipeline instances"
    classification: fp
    notes: "17 hits — mostly FP, pipeline instances managed via parent lifecycle"
  - id: "resource-tracking/no-destroy-method::utility classes"
    classification: fp
    notes: "10 hits — utility classes use disconnect_all(), remove() instead"
  - id: "R-SLOP-38::descriptive identifier"
    classification: fp
    notes: "dash_not_already_destroyed is descriptive, not AI verbosity. Fixed: guard-pattern."
  - id: "R-SLOP-24::non-extension schema"
    classification: fp
    notes: "new Gio.Settings({schema: 'org.gnome.mutter'}) correctly accesses system schema. Fixed: guard-pattern."
  # WARNs — True Positives
  - id: "lifecycle/prototype-override::UnlockDialog overrides"
    classification: tp
    notes: "6 instances — correctly flags lockscreen UnlockDialog overrides"
  - id: "R-I18N-01::template literals in _()"
    classification: tp
    notes: "4 instances — breaks xgettext extraction"
  - id: "R-SLOP-16::GLib.file_get_contents synchronous"
    classification: tp
    notes: "Synchronous file read advisory"
  - id: "R-SLOP-03::version field deprecated"
    classification: tp
    notes: "Deprecated for GNOME 45+"
  - id: "R-SEC-09::Main.extensionManager access"
    classification: tp
    notes: "Extension system interference for Dash to Panel compat"
  - id: "R-DEPR-09::var usage"
    classification: tp
    notes: "var x, y; should use let"
  - id: "quality/private-api::Main.overview._overview"
    classification: tp
    notes: "Private API access — correct advisory"
  - id: "quality/module-state::module vars not reset"
    classification: tp
    notes: "sigma and brightness not reset in disable"
  - id: "quality/empty-catch::empty catch block"
    classification: tp
    notes: "paint_signals.js — empty catch"
  - id: "lifecycle/signal-balance::125 vs 28"
    classification: tp
    notes: "By design — Connections class auto-cleans, but signal-balance heuristic can't verify"
  - id: "lifecycle/async-destroyed-guard::await import"
    classification: tp
    notes: "Low risk — await import() in utils.js without guard"
  # WARNs — Mixed
  - id: "lifecycle/untracked-timeout::prefs auto-cleanup"
    classification: borderline
    notes: "4 hits — 2 TP, 2 FP (prefs.js timeouts auto-cleanup on window close)"
  # New in 03-08 (lint gap closure)
  - id: "lifecycle/gsettings-signal-leak::prefs bare connect"
    classification: tp
    notes: "1 bare settings.connect() in src/preferences — no disconnectObject cleanup"
  - id: "resource-tracking/ownership::orphan count 12→11"
    classification: borderline
    notes: "Orphan count reduced from 12 to 11 after no-destroy-method FP reduction (PR #86)"
  # Resolved in 03-08
  - id: "init/shell-modification::appfolders.js exempted"
    classification: resolved
    notes: "FAIL resolved — appfolders.js exempted by init/shell-modification expansion (PR #88)"
  - id: "resource-tracking/no-destroy-method::settings.js resolved"
    classification: resolved
    notes: "WARN resolved — settings.js cleanup pattern now recognized (PR #86)"
  # ego-review advisory findings (now partially caught by ego-lint)
  - id: "lifecycle::actor.destroy missing parentheses"
    classification: tp
    notes: "L-3: coverflow_alt_tab.js:69 — actor.destroy is property access, not function call. Now caught by R-LIFE-23 lint check."
  - id: "lifecycle::splice wrong argument type"
    classification: tp
    notes: "L-10: window_list.js:111 — passes object instead of index to splice()"
  - id: "lifecycle::setTimeout source ID not stored"
    classification: tp
    notes: "L-1: panel.js:70 — can fire after disable"
  - id: "lifecycle::GLib.idle_add source ID not stored"
    classification: tp
    notes: "L-2: panel.js:91 — can fire after disable"

  # New annotations — exact baseline IDs (2026-03-10)
  # SKIP findings — expected (not applicable)
  - id: "polkit/action-id-match::No polkit files found"
    classification: expected
    notes: "No polkit files in extension"
  - id: "polkit/action-id-prefix::No polkit files found"
    classification: expected
    notes: "No polkit files in extension"
  - id: "polkit/helper-exists::No polkit files found"
    classification: expected
    notes: "No polkit files in extension"
  - id: "R-WEB-01::Not applicable for declared shell-version(s)"
    classification: expected
    notes: "Version-gated skip — extension targets GNOME 45+"
  - id: "R-WEB-02::Not applicable for declared shell-version(s)"
    classification: expected
    notes: "Version-gated skip — extension targets GNOME 45+"
  - id: "R-WEB-10::Not applicable for declared shell-version(s)"
    classification: expected
    notes: "Version-gated skip — extension targets GNOME 45+"
  - id: "R-WEB-11::Not applicable for declared shell-version(s)"
    classification: expected
    notes: "Version-gated skip — extension targets GNOME 45+"
  - id: "R-DEPR-04-legacy::Not applicable for declared shell-version(s)"
    classification: expected
    notes: "Version-gated skip — extension targets GNOME 45+"
  - id: "R-VER50-01::Not applicable for declared shell-version(s)"
    classification: expected
    notes: "Extension does not declare GNOME 50 support"
  - id: "R-VER50-02::Not applicable for declared shell-version(s)"
    classification: expected
    notes: "Extension does not declare GNOME 50 support"
  - id: "R-VER50-03::Not applicable for declared shell-version(s)"
    classification: expected
    notes: "Extension does not declare GNOME 50 support"
  - id: "R-VER50-04::Not applicable for declared shell-version(s)"
    classification: expected
    notes: "Extension does not declare GNOME 50 support"
  - id: "R-VER50-05::Not applicable for declared shell-version(s)"
    classification: expected
    notes: "Extension does not declare GNOME 50 support"
  - id: "eslint::No eslint.config.mjs or node_modules/.bin/eslint found"
    classification: expected
    notes: "Optional tooling not present"
  - id: "schema-usage/unused-key::Dynamic key access detected — cannot verify statically"
    classification: expected
    notes: "Static analysis limitation — extension uses dynamic key access"
  - id: "schema-usage/undefined-key::Dynamic key access detected — cannot verify statically"
    classification: expected
    notes: "Static analysis limitation — extension uses dynamic key access"
  - id: "package/exists::No zip package found in extension directory"
    classification: expected
    notes: "Source checkout, not packaged"
  - id: "metadata/uuid-matches-dir::UUID 'blur-my-shell@aunetx' does not match directory 'blur-my-shell' — must match when installed"
    classification: expected
    notes: "Dev/cache directory name differs from UUID"
  # FAIL findings — True Positives
  - id: "R-DEPR-06::Tweener is removed; use Clutter animation framework"
    classification: tp
    notes: "appfolders.js uses Tweener — crashes on GNOME 46+"
  - id: "lifecycle/gsettings-signal-leak::1 bare settings.connect('changed::...') without stored ID and no disconnectObject/connectObject cleanup: src/preferences/applications_management/application_row.js"
    classification: resolved
    notes: "Bare settings.connect() in preferences without disconnectObject cleanup — no longer emitted"
  # WARN findings — True Positives
  - id: "css/important::Found 9 !important usage(s) in stylesheet.css — !important overrides Shell theme; prefer higher specificity"
    classification: tp
    notes: "Valid advisory — !important overrides Shell theme styling"
  - id: "R-DEPR-09::Use const/let instead of var; var has function scope and causes bugs in closures"
    classification: tp
    notes: "var usage should use const/let"
  - id: "R-SEC-09::Extension system interference is discouraged; modifying other extensions requires justification"
    classification: tp
    notes: "Accesses Main.extensionManager for Dash to Panel compat"
  - id: "R-I18N-01::Template literal inside gettext _() breaks xgettext string extraction"
    classification: tp
    notes: "4 instances — template literals break xgettext extraction"
  - id: "R-SLOP-03::version field is deprecated; EGO manages versions for GNOME 45+"
    classification: tp
    notes: "Version field deprecated for GNOME 45+"
  - id: "R-SLOP-16::GLib.file_get_contents() is synchronous and blocks the main loop; prefer Gio.File.load_contents() or load_contents_async()"
    classification: tp
    notes: "Synchronous file read blocks main loop"
  - id: "metadata/non-standard-field::'original-authors' is not a standard metadata field"
    classification: tp
    notes: "Non-standard metadata field advisory"
  - id: "metadata/deprecated-version::version field is ignored by EGO for GNOME 45+; consider removing"
    classification: tp
    notes: "Version field deprecated for GNOME 45+"
  - id: "quality/module-state::Module-level mutable state at src/components/appfolders.js, src/components/appfolders.js — ensure reset in both enable() and disable()"
    classification: tp
    notes: "Module-level sigma/brightness vars not reset in disable()"
  - id: "quality/empty-catch::empty catch block — at minimum log with console.debug()"
    classification: tp
    notes: "paint_signals.js:28 — empty catch swallows errors"
  - id: "quality/private-api::src/extension.js, src/components/panel.js, src/components/panel.js, src/components/appfolders.js, src/components/appfolders.js (+1 more): Main.layoutManager private API access — requires reviewer justification and version pinning"
    classification: tp
    notes: "Private API access — valid advisory for version-sensitive code"
  - id: "lifecycle/signal-balance::30 manual .connect() calls but only 28 .disconnect() calls — verify all signals are disconnected in disable()"
    classification: resolved
    notes: "Connections class auto-cleans but heuristic can't verify — no longer emitted"
  - id: "lifecycle/async-destroyed-guard::async/await used without _destroyed or _isDestroyed guard — extension may act on stale state after disable()"
    classification: tp
    notes: "Low risk — await import() without destroyed guard"
  - id: "lifecycle/prototype-override::UnlockDialog.prototype._createBackground — direct prototype modification should be restored in disable()"
    classification: tp
    notes: "Lockscreen prototype override — must be restored in disable()"
  - id: "lifecycle/prototype-override::UnlockDialog.prototype._updateBackgroundEffects — direct prototype modification should be restored in disable()"
    classification: tp
    notes: "Lockscreen prototype override — must be restored in disable()"
  - id: "lifecycle/prototype-override::UnlockDialog.prototype._updateBackgrounds — direct prototype modification should be restored in disable()"
    classification: tp
    notes: "Lockscreen prototype override — must be restored in disable()"
  - id: "lifecycle/destroy-no-call::.destroy without () — property access, not method call"
    classification: tp
    notes: "coverflow_alt_tab.js:69 — .destroy property access, not method call"
  - id: "resource-tracking/orphan-signal::src/components/appfolders.js — this._sourceMappedId created but not cleaned up in destroy()"
    classification: tp
    notes: "Signal ID stored but never disconnected in cleanup"
  - id: "resource-tracking/orphan-gsettings::src/components/applications.js — this.mutter_gsettings created but not cleaned up in destroy()"
    classification: tp
    notes: "GSettings instance created in enable() but not cleaned up"
  # WARN findings — False Positives
  - id: "resource-tracking/no-destroy-method::src/conveniences/paint_signals.js — no cleanup method (destroy/disable/onDestroy) in src/conveniences/paint_signals.js"
    classification: resolved
    notes: "Class uses disconnect_all() for cleanup — no longer emitted"
  - id: "resource-tracking/no-destroy-method::src/conveniences/effects_manager.js — no cleanup method (destroy/disable/onDestroy) in src/conveniences/effects_manager.js"
    classification: fp
    notes: "Utility class — cleanup managed via parent lifecycle"
  - id: "resource-tracking/no-destroy-method::src/effects/upscale.js — no cleanup method (destroy/disable/onDestroy) in src/effects/upscale.js"
    classification: resolved
    notes: "GObject effect — no longer emitted"
  - id: "resource-tracking/no-destroy-method::src/effects/downscale.js — no cleanup method (destroy/disable/onDestroy) in src/effects/downscale.js"
    classification: resolved
    notes: "GObject effect — no longer emitted"
  - id: "resource-tracking/no-destroy-method::src/effects/gaussian_blur.js — no cleanup method (destroy/disable/onDestroy) in src/effects/gaussian_blur.js"
    classification: resolved
    notes: "GObject effect — no longer emitted"
  # WARN findings — Borderline
  - id: "quality/constructor-resources::.connect() in constructor — move to enable()"
    classification: borderline
    notes: "3 hits — settings.js uses dynamic accessor pattern with cleanup; effects_dialog.js is prefs GTK widget (auto-cleanup)"
  - id: "lifecycle/untracked-timeout::timer/idle return value not stored — cannot be removed in disable()"
    classification: borderline
    notes: "4 hits — 2 in panel.js (TP), 2 in prefs code (auto-cleanup on window close)"
  - id: "resource-tracking/ownership::48 files scanned, depth 3, 11 orphans detected"
    classification: resolved
    notes: "Summary orphan count — superseded by updated 7-orphan finding"
  # New annotations (2026-03-11) — R-SLOP-11 split / resource graph improvements
  - id: "lifecycle/module-scope-prototype::Signals.addSignalMethods(PipelinesManager.prototype) — prototype mutation at module scope persists after disable()"
    classification: tp
    notes: "Signals.addSignalMethods() adds emit/connect/disconnect to prototype at module scope — persists after disable()"
  - id: "lifecycle/module-scope-prototype::Signals.addSignalMethods(Settings.prototype) — prototype mutation at module scope persists after disable()"
    classification: tp
    notes: "Signals.addSignalMethods() adds emit/connect/disconnect to prototype at module scope — persists after disable()"
  - id: "resource-tracking/destroy-not-called::src/conveniences/paint_signals.js — parent does not call cleanup method on src/conveniences/paint_signals.js"
    classification: fp
    notes: "PaintSignals uses disconnect_all() for cleanup, called by parent components — resource graph does not recognize this method name"
  - id: "resource-tracking/ownership::48 files scanned, depth 3, 7 orphans detected"
    classification: borderline
    notes: "Orphan count reduced from 11 to 7 after no-destroy-method improvements — remaining count includes some FP findings"
